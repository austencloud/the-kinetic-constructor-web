import React from "react";
import styles from "./TurnsBoxStyles.module.css"; // Reuse the consolidated styles

const TurnsTextLabel = () => {
  return <div className={styles.turnsTextLabel}>Turns</div>;
};

export default TurnsTextLabel;
